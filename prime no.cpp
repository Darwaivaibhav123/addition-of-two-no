
#include <iostream>

using namespace std;

int main()
{
      int a  ;
  cout << "enter a  no =";
  cin>>a ;
  if (a==1 )
  cout << "given no is not prime no";

   else if (a==2 )
  cout<<"given no is prime no ";
  else if (a%2!=0  )
  cout<<"given no is prime no ";
   else   
   cout << "given no is not prime no";

    return 0;
}
